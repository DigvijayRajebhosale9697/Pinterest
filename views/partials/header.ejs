<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pinterest</title>
  <link rel="shortcut icon" href="/images/Pin.png" type="image/x-icon">
  <link href="https://cdn.jsdelivr.net/npm/remixicon@4.0.0/fonts/remixicon.css" rel="stylesheet" />
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
  <% if (nav===true){ %>
    <div class="w-full px-10 py-3 flex justify-between bg-zinc-800 text-white items-center">
      <img src="/images/Pin.png" alt="Pinterest Logo" class="w-10 h-10 mb-4 mr-4">
      <div class="flex gap-10 items-center text-gray-400">
        <a href="/profile"
          class="<%= currentURL === '/profile' ? 'text-white' : 'text-gray-400' %>  hover:text-white active:text-white">Profile</a>
        <a href="/feed" class="<%= currentURL === '/feed' ? 'text-white' : 'text-gray-400' %> hover:text-white">Feed</a>
        <a class="<%= currentURL === '/logout' ? 'text-white' : 'text-gray-400' %> hover:text-white px-2 py-3 bg-red-500 text-white rounded-lg"
          href="/logout">Logout</a>
      </div>
    </div>
    <% } %>